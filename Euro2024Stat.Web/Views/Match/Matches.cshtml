@model IEnumerable<MatchDto>;
@using Euro2024Stat.Web.Helper;

@{

    List<string> GroupMatches = new List<string>() { "A", "B", "C", "D", "E", "F" };
}

<div class="row">
    <div class="col-md-12">
        <h3>Group Stage Upcoming Matches</h3>

        <div class="row">
            @foreach (var match in Model.Where(x => GroupMatches.Contains(x.Group)))
            {
                <div class="col-md-4 mb-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <img src="@Url.Content($"~/img/Country/{match.HomeCountryID}/{match.HomeCountryID}.jpg")" alt="@match.HomeCountryID" height="20" width="30" />
                                VS  <img src="@Url.Content($"~/img/Country/{match.AwayCountryID}/{match.AwayCountryID}.jpg")" alt="@match.AwayCountryID" height="20" width="30" />

                            </h5>
                            <p class="card-text">
                                <strong>Stadium:</strong> @match.Stadium<br>
                                <strong>Date:</strong> @match.MatchDatetime<br>
                            </p>
                        </div>
                        @if (User.IsInRole(ApiHelper.RoleAdmin))
                        {
                            <form asp-controller="AdminEntities" asp-action="FinishMatch" method="post">
                                <input type="hidden" name="matchId" value="@match.ID" />
                                <button type="submit" class="btn btn-success">
                                    <i class="bi bi-check-square"> Finish Match</i>
                                </button>
                            </form>
                        }

                    </div>
                </div>
            }
        </div>

        <br />
        <br />
        <br />
        <h3>Round 8 Upcoming Matches</h3>

        <div class="row">
            @foreach (var match in Model.Where(x => x.Group == "round8"))
            {
                <div class="col-md-4 mb-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <img src="@Url.Content($"~/img/Country/{match.HomeCountryID}/{match.HomeCountryID}.jpg")" alt="@match.HomeCountryID" height="20" width="30" />
                                VS  <img src="@Url.Content($"~/img/Country/{match.AwayCountryID}/{match.AwayCountryID}.jpg")" alt="@match.AwayCountryID" height="20" width="30" />

                            </h5>
                            <p class="card-text">
                                <strong>Stadium:</strong> @match.Stadium<br>
                                <strong>Date:</strong> @match.MatchDatetime<br>
                            </p>
                        </div>
                        @if (User.IsInRole(ApiHelper.RoleAdmin))
                        {
                            <form asp-controller="AdminEntities" asp-action="FinishMatch" method="post">
                                <input type="hidden" name="matchId" value="@match.ID" />
                                <button type="submit" class="btn btn-success">
                                    <i class="bi bi-check-square"> Finish Match</i>
                                </button>
                            </form>
                        }

                    </div>
                </div>
            }
        </div>

        <br />
        <br />
        <br />
        <h3>Round 4 Upcoming Matches</h3>

        <div class="row">
            @foreach (var match in Model.Where(x => x.Group == "round4"))
            {
                <div class="col-md-4 mb-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <img src="@Url.Content($"~/img/Country/{match.HomeCountryID}/{match.HomeCountryID}.jpg")" alt="@match.HomeCountryID" height="20" width="30" />
                                VS  <img src="@Url.Content($"~/img/Country/{match.AwayCountryID}/{match.AwayCountryID}.jpg")" alt="@match.AwayCountryID" height="20" width="30" />

                            </h5>
                            <p class="card-text">
                                <strong>Stadium:</strong> @match.Stadium<br>
                                <strong>Date:</strong> @match.MatchDatetime<br>
                            </p>
                        </div>
                        @if (User.IsInRole(ApiHelper.RoleAdmin))
                        {
                            <form asp-controller="AdminEntities" asp-action="FinishMatch" method="post">
                                <input type="hidden" name="matchId" value="@match.ID" />
                                <button type="submit" class="btn btn-success">
                                    <i class="bi bi-check-square"> Finish Match</i>
                                </button>
                            </form>
                        }

                    </div>
                </div>
            }
        </div>

        <br />
        <br />
        <br />
        <h3>Semi-Final Upcoming Matches</h3>

        <div class="row">
            @foreach (var match in Model.Where(x => x.Group == "round2"))
            {
                <div class="col-md-4 mb-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <img src="@Url.Content($"~/img/Country/{match.HomeCountryID}/{match.HomeCountryID}.jpg")" alt="@match.HomeCountryID" height="20" width="30" />
                                VS  <img src="@Url.Content($"~/img/Country/{match.AwayCountryID}/{match.AwayCountryID}.jpg")" alt="@match.AwayCountryID" height="20" width="30" />

                            </h5>
                            <p class="card-text">
                                <strong>Stadium:</strong> @match.Stadium<br>
                                <strong>Date:</strong> @match.MatchDatetime<br>
                            </p>
                        </div>
                        @if (User.IsInRole(ApiHelper.RoleAdmin))
                        {
                            <form asp-controller="AdminEntities" asp-action="FinishMatch" method="post">
                                <input type="hidden" name="matchId" value="@match.ID" />
                                <button type="submit" class="btn btn-success">
                                    <i class="bi bi-check-square"> Finish Match</i>
                                </button>
                            </form>
                        }

                    </div>
                </div>
            }
        </div>

        <br />
        <br />
        <br />
        <h3>Final Upcoming Match</h3>

        <div class="row">
            @foreach (var match in Model.Where(x => x.Group == "final"))
            {
                <div class="col-md-4 mb-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <img src="@Url.Content($"~/img/Country/{match.HomeCountryID}/{match.HomeCountryID}.jpg")" alt="@match.HomeCountryID" height="20" width="30" />
                                VS  <img src="@Url.Content($"~/img/Country/{match.AwayCountryID}/{match.AwayCountryID}.jpg")" alt="@match.AwayCountryID" height="20" width="30" />

                            </h5>
                            <p class="card-text">
                                <strong>Stadium:</strong> @match.Stadium<br>
                                <strong>Date:</strong> @match.MatchDatetime<br>
                            </p>
                        </div>
                        @if (User.IsInRole(ApiHelper.RoleAdmin))
                        {
                            <form asp-controller="AdminEntities" asp-action="FinishMatch" method="post">
                                <input type="hidden" name="matchId" value="@match.ID" />
                                <button type="submit" class="btn btn-success">
                                    <i class="bi bi-check-square"> Finish Match</i>
                                </button>
                            </form>
                        }

                    </div>
                </div>
            }
        </div>

    </div>
</div>
